# Используем официальный образ Node.js на базе Alpine Linux.
# Alpine Linux - это минималистичная версия Linux, которая позволяет создавать легковесный контейнер.
FROM node:alpine

# Устанавливаем рабочую директорию для приложения в контейнере.
# Все команды, выполняемые ниже, будут относиться к этой директории.
WORKDIR /time-app

# Копируем файлы package.json и package-lock.json (если они есть) в рабочую директорию.
# Это позволяет сначала установить только зависимости, что оптимизирует сборку.
COPY package*.json ./

# Устанавливаем зависимости проекта, используя npm.
# 'npm install' устанавливает все пакеты, указанные в package.json, необходимые для работы приложения.
RUN npm install

# Копируем весь исходный код приложения в текущую рабочую директорию контейнера.
# Это добавляет все оставшиеся файлы от проекта в контейнер.
COPY . .

# Открываем порт 5000 для доступа к приложению из хоста.
# Это порт, на котором будет работать сервер Node.js.
EXPOSE 5000

# Указываем команду для запуска приложения.
# Команда 'npm run dev' запускает проект в режиме разработки.
CMD ["npm", "run", "dev"]
